---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synology-media
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology
  resources:
    requests:
      storage: 5000Gi

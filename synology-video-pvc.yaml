---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: synology-video
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: synology
  resources:
    requests:
      storage: 5000Gi
